Don't remember how to remove and append elements? Let's review the following procedures:

* `list.append(list, item)`: adds `item` to the end of `list`
* `list.remove(list, element)`: removes `element` from `list`
